on_game_start = {
    on_actions = {
		IBL_setup_on_actions
		IBL_Var
    }
}

IBL_Var = {
	effect = {
		set_global_variable = {
			name = ibl_is_loaded
			value = yes
		}
		if = { #Error Destroyer 5000 #this is the best
			limit = {
				exists = global_var:ibl_is_loaded
			}
			# Do nothing
		}
	}
}

IBL_setup_on_actions = {
	effect = {
		if = {
			limit = {
				bap_is_loaded = yes
			}
		IBL_BAP_patch_effect = yes
		IBL_BAP_west_africa_setup_effects = yes
		}
	}
}